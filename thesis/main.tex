%% ============================================================
%%  GNN-AD-Navigator — Master's Thesis (PFE M2)
%%  FST Tangier — Academic Year 2025/2026
%%  Author : Nizar Senbati
%%  Compile : pdflatex → bibtex → pdflatex × 2
%% ============================================================

\documentclass[12pt, a4paper, oneside]{report}

%% ── Encoding & Language ──────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}

%% ── Typography ───────────────────────────────────────────────
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{setspace}
\onehalfspacing

%% ── Page Geometry ────────────────────────────────────────────
\usepackage[
  top=2.5cm, bottom=2.5cm,
  left=3cm,  right=2.5cm,
  headheight=15pt
]{geometry}

%% ── Headers & Footers ────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\leftmark}
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}

%% ── Colours ──────────────────────────────────────────────────
\usepackage[dvipsnames, table]{xcolor}
\definecolor{thesisblue}{RGB}{31, 78, 121}   % #1f4e79  (GUI navy)
\definecolor{lightgray}{RGB}{245, 245, 245}
\definecolor{codegray}{RGB}{240, 240, 240}

%% ── Graphics & Figures ───────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\graphicspath{{figures/}}   % put all images in ./figures/

%% ── Tables ───────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{array}
\usepackage{longtable}

%% ── Maths ────────────────────────────────────────────────────
\usepackage{amsmath, amssymb, amsthm}

%% ── Code Listings ────────────────────────────────────────────
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codegray},
  frame=single,
  rulecolor=\color{gray!40},
  breaklines=true,
  numbers=left,
  numberstyle=\tiny\color{gray},
  keywordstyle=\color{thesisblue}\bfseries,
  commentstyle=\color{OliveGreen},
  stringstyle=\color{BrickRed},
  showstringspaces=false,
  captionpos=b,
}
\lstdefinelanguage{cypher}{
  keywords={MATCH, WHERE, RETURN, WITH, AS, AND, OR, NOT, LIMIT},
  sensitive=false,
  morecomment=[l]{//},
  morestring=[b]",
}

%% ── Algorithms ───────────────────────────────────────────────
\usepackage[ruled, vlined, linesnumbered]{algorithm2e}

%% ── TikZ / PGF (for pipeline diagrams) ──────────────────────
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds}
%% ── Thesis colour palette ────────────────────────────────────
\definecolor{navyblue}{RGB}{31,78,121}
\definecolor{navylight}{RGB}{52,114,168}
\definecolor{slategray}{RGB}{100,120,140}
\definecolor{boxbg}{RGB}{235,242,250}
\definecolor{boxborder}{RGB}{180,210,240}
\definecolor{arrowgray}{RGB}{80,100,120}
\definecolor{labelgray}{RGB}{60,80,100}
\definecolor{forestgreen}{RGB}{34,100,60}
\definecolor{forestbg}{RGB}{230,245,235}
\definecolor{domainbg}{RGB}{220,235,250}
\definecolor{oubg}{RGB}{235,235,255}
\definecolor{oucolor}{RGB}{70,70,180}
\definecolor{objbg}{RGB}{250,250,250}

%% ── Circled step number ──────────────────────────────────────
\newcommand{\stepnum}[1]{%
  \tikz[baseline=(n.base)]\node[circle,
    draw=navyblue, fill=navyblue, text=white,
    font=\scriptsize\bfseries,
    inner sep=1.5pt, minimum size=0.5cm] (n) {#1};}

%% ── Common styles ────────────────────────────────────────────
\tikzset{
  forestbox/.style={
    draw=forestgreen, fill=forestbg, line width=1.2pt,
    rounded corners=5pt, minimum width=3.2cm,
    minimum height=0.85cm, align=center,
    font=\small\bfseries\color{forestgreen}
  },
  domainbox/.style={
    draw=navyblue, fill=domainbg, line width=1.0pt,
    rounded corners=4pt, minimum width=2.8cm,
    minimum height=0.8cm, align=center,
    font=\small\bfseries\color{navyblue}
  },
  oubox/.style={
    draw=oucolor, fill=oubg, line width=0.8pt,
    rounded corners=3pt, minimum width=2.4cm,
    minimum height=0.7cm, align=center,
    font=\small\color{oucolor}
  },
  objbox/.style={
    draw=slategray, fill=objbg, line width=0.6pt,
    rounded corners=2pt, minimum width=1.9cm,
    minimum height=0.65cm, align=center,
    font=\scriptsize\color{labelgray}
  },
  entitybox/.style={
    draw=navyblue, fill=domainbg, line width=1.2pt,
    rounded corners=5pt, minimum width=2.4cm,
    minimum height=1.1cm, align=center,
    font=\small\bfseries\color{navyblue}
  },
  msgbox/.style={
    draw=boxborder, fill=boxbg, line width=0.8pt,
    rounded corners=3pt,
    font=\scriptsize\color{labelgray},
    inner sep=5pt, align=left
  },
  thesisarrow/.style={
    -{Stealth[length=6pt,width=4pt]},
    draw=arrowgray, line width=1.0pt
  },
  dasharrow/.style={
    -{Stealth[length=5pt,width=3.5pt]},
    draw=arrowgray, dashed, line width=0.9pt
  },
  bidiarrow/.style={
    {Stealth[length=5pt,width=3.5pt]}-{Stealth[length=5pt,width=3.5pt]},
    draw=arrowgray, line width=1.0pt
  },
  arrowlabel/.style={
    font=\scriptsize\color{labelgray},
    fill=white, inner sep=2pt
  },
  diagramtitle/.style={
    font=\large\bfseries\color{navyblue}
  },
}

%% ── Hyperlinks & PDF Metadata ────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=thesisblue,
  citecolor=thesisblue,
  urlcolor=thesisblue,
  pdftitle={Learning Offensive Navigation Policies on Heterogeneous Attack Graphs},
  pdfauthor={Nizar Senbati},
  pdfsubject={Master PFE Thesis},
  pdfkeywords={GNN, Active Directory, Attack Graph, Privilege Escalation, HGT}
]{hyperref}
\usepackage{cleveref}   % \cref{} for smart cross-references

%% ── Bibliography ─────────────────────────────────────────────
\usepackage[
  backend=bibtex,
  style=ieee,
  sorting=none,
  maxbibnames=6
]{biblatex}
\addbibresource{references.bib}

%% ── Acronyms ─────────────────────────────────────────────────
\usepackage[acronym, toc, nomain]{glossaries}
\makeglossaries
\newacronym{ad}{AD}{Active Directory}
\newacronym{gnn}{GNN}{Graph Neural Network}
\newacronym{hgt}{HGT}{Heterogeneous Graph Transformer}
\newacronym{gcn}{GCN}{Graph Convolutional Network}
\newacronym{pyg}{PyG}{PyTorch Geometric}
\newacronym{acl}{ACL}{Access Control List}
\newacronym{adcs}{ADCS}{Active Directory Certificate Services}
\newacronym{rbcd}{RBCD}{Resource-Based Constrained Delegation}
\newacronym{sid}{SID}{Security Identifier}
\newacronym{ou}{OU}{Organisational Unit}
\newacronym{gpo}{GPO}{Group Policy Object}
\newacronym{ntlm}{NTLM}{NT LAN Manager}
\newacronym{da}{DA}{Domain Admin}
\newacronym{poc}{PoC}{Proof of Concept}
\newacronym{rl}{RL}{Reinforcement Learning}
\newacronym{mlp}{MLP}{Multi-Layer Perceptron}
\newacronym{goad}{GOAD}{Game of Active Directory}
\newacronym{dc}{DC}{Domain Controller}
\newacronym{dcsync}{DCSync}{Directory Replication Service synchronisation attack}

%% ── Custom Commands ──────────────────────────────────────────
\newcommand{\tool}[1]{\texttt{#1}}
\newcommand{\edge}[1]{\textsc{\small #1}}
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}
% \todo{} marks — remove all before submission

%% ── Chapter Title Style ──────────────────────────────────────
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\color{thesisblue}}
  {\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titlespacing*{\chapter}{0pt}{10pt}{30pt}

%% ============================================================
\begin{document}

%% ── TITLE PAGE ───────────────────────────────────────────────
\begin{titlepage}
  \centering
  \includegraphics[width=0.35\textwidth]{fst_logo.png}\\[1cm]
  % Replace fst_logo with your actual logo filename (no extension needed if .png/.pdf)

  {\large\bfseries Faculté des Sciences et Techniques de Tanger}\\[0.3cm]
  {\large Université Abdelmalek Essaadi}\\[0.5cm]
  \rule{\textwidth}{0.5pt}\\[0.5cm]

  {\large Master's End-of-Study Project (PFE)}\\
  {\large Master in \todo{your master programme name}}\\[1.5cm]

  {\Huge\bfseries\color{thesisblue}
    Learning Offensive Navigation Policies\\
    on Heterogeneous Attack Graphs}\\[0.8cm]

  {\Large A GNN Approach to Sequential Privilege Escalation\\
    in Active Directory}\\[2cm]

  \begin{minipage}[t]{0.45\textwidth}
    \raggedright
    \textbf{Author:}\\
    Nizar Senbati
  \end{minipage}
  \hfill
  \begin{minipage}[t]{0.45\textwidth}
    \raggedleft
    \textbf{Supervisor:}\\
    \todo{Supervisor name \& title}
  \end{minipage}\\[2cm]

  \rule{\textwidth}{0.5pt}\\[0.5cm]
  {\large Academic Year 2025 -- 2026}
\end{titlepage}

%% ── FRONT MATTER ─────────────────────────────────────────────
\pagenumbering{roman}
\setcounter{page}{1}

%% ── Acknowledgements ─────────────────────────────────────────
\chapter*{Acknowledgements}
\addcontentsline{toc}{chapter}{Acknowledgements}

First and foremost, I would like to express my deepest gratitude to my academic supervisor
GHADI Abderahim, for his unwavering guidance, patience, and invaluable feedback
throughout this academic year.
\\
I am equally grateful to my professional supervisor, ZAABOUL Jawad, and the 
entire team at NearSecure. Thank you for providing an environment that fosters innovation, 
for trusting me to pursue the intersection of offensive security and Deep Learning, 
and for the technical mentorship that bridged the gap between academic theory and real-world application.
\\
A massive thank you is owed to the broader cybersecurity and open-source communities. 
The development of the GNN-AD-Navigator would not have been possible without the tireless 
work of the developers who build and maintain the tools we rely on. Specifically, I would like to acknowledge:
\\
SpecterOps, for creating and open-sourcing BloodHound, which provided the foundational graph 
topology and data schema necessary for my model's ingestion pipeline.
\\
Oliver Lyak ly4k, for developing Certipy, an essential tool that allowed me to accurately 
map complex Active Directory Certificate Services ADCS attack paths into my heterogeneous graph.
\\
Mayfly Orange Cyberdefense, for creating GOAD Game of Active Directory. The immense complexity 
of this lab environment served as the perfect training ground for the neural network, allowing it 
to learn and discover emergent tactical behaviors.
\\
I also want to thank Hack The Box Academy for their student program, which granted me 
accessible access to the inlanefreight Active Directory environment. This provided the critical 
cross-environment validation required to prove the model's efficacy outside of its training data.
\\
Finally, a personal thank you to my father and familly for the ongoing support, advice and guidance during 
the long hours spent debugging and studying. 
To everyone who supported me through this Master's journey: thank you.

%% ── Abstract (English) ───────────────────────────────────────
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}

% ~150 words. Cover: problem (AD attack path analysis), approach (GNN policy on
% heterogeneous graph), key result (cross-environment generalisation on INLANEFREIGHT),
% contribution (learned policy vs. shortest path), and honest scope (PoC).
Active Directory is a directory service so deeply embedded in corporate 
infrastructure that misconfigurations, no matter how small, can expose 
entire organisations to privilege escalation attacks. Tools like BloodHound 
are critical for both defenders and red teams: they map the environment as a 
graph and let operators query attack paths between any two objects. 
BloodHound's built-in pathfinding, however, runs plain Dijkstra --- it returns 
the shortest path, not necessarily the most operationally sound one, and it 
cannot learn from prior engagements.

This thesis asks whether a Graph Neural Network can learn a navigation policy 
from labelled attack traces and generalise it to entirely unseen enterprise 
environments. A Heterogeneous Graph Transformer is trained on a multi-domain 
lab environment and evaluated on a 4\,000-node enterprise network it has 
never seen. The model recovers the expert-documented Domain Admin attack path 
without retraining, establishing cross-environment generalisation as a proof 
of concept. Identified architectural boundaries and a forward-looking 
hypothesis for a second-generation tool are discussed.

\vspace{1.5cm}
\noindent\textbf{Keywords:} Graph Neural Network, Heterogeneous Graph Transformer,
Active Directory, Privilege Escalation, Attack Graph, Beam Search,
Offensive Security, Cross-Environment Generalisation, Active Directory Cerfication Services.

%% ── Abstract (French) ────────────────────────────────────────
\chapter*{Résumé}
\addcontentsline{toc}{chapter}{Résumé}

Active Directory est un service d'annuaire si profondément intégré dans l'infrastructure 
des entreprises que la moindre erreur de configuration peut exposer une organisation entière 
à des attaques de privilege escalation. Des outils comme BloodHound sont essentiels 
à la fois pour les défenseurs et les red teams : ils cartographient l'environnement sous forme 
de graphe et permettent aux opérateurs de rechercher des chemins d'attaque entre deux objets.
Cependant, le moteur de recherche de BloodHound utilise le simple algorithme de Dijkstra 
renvoie le chemin le plus court, et non le plus pertinent d'un point de vue opérationnel.

Cette thèse cherche à savoir si un Graph Neural Network peut apprendre une politique de navigation
à partir de traces d'attaques étiquetées, et la généraliser à des environnements d'entreprise 
totalement inconnus. Un Heterogeneous Graph Transformer est entraîné sur un environnement de 
laboratoire multi-domaines, puis évalué sur un réseau d'entreprise de 4 000 nœuds qu'il 
n'a jamais vu auparavant. Le modèle parvient à retrouver le chemin d'attaque Domain Admin 
documenté par des experts, et ce sans réentraînement, validant ainsi la généralisation 
cross-environment comme un véritable Proof of Concept. Enfin, les limites architecturales 
identifiées ainsi que des pistes d'évolution pour un outil de seconde génération sont discutées.

\vspace{1.5cm}
\noindent\textbf{Mots-clés :} Réseau de neurones sur graphe, Transformateur de graphe hétérogène,
Active Directory, Escalade de privilèges, Graphe d'attaque, Recherche en faisceau,
Sécurité offensive, Généralisation inter-environnements.

%% ── Tables of Contents / Figures / Tables ────────────────────
\tableofcontents
\listoffigures
\listoftables

%% ── Abbreviations (auto-generated from glossaries) ──────────
\printglossary[type=\acronymtype, title=List of Abbreviations]

\cleardoublepage
\pagenumbering{arabic}
\setcounter{page}{1}

%% ============================================================
%%  GENERAL INTRODUCTION
%% ============================================================
\chapter*{General Introduction}
\addcontentsline{toc}{chapter}{General Introduction}
\markboth{General Introduction}{}

\section*{Context}

Active Directory is the identity backbone of major enterprises. While typically isolated withing 
the inside network, once the perimeter defenses are breached 
(e.g., via web service exploitation, phishing, or a compromised usb) an advarsary gains the 
ability to directly interacte and query the Active Directory Domain Controller. Through protocols
such as LDAP and SMB the entirety  of the organisation's IT infrastructure , data and services 
is essencialy mapped out for the attacker.
A vulnerable AD poses extreme levels of risk making hardening the AD network environment an extreme priority.


Once inside, attackers frequently leverage credential-based and ACL-based attacks such as 
pass-the-hash, DCSync, Kerberoasting are among the most impactful attack vectors in modern breaches.
Red teams and security auditor alike rely on tools like BloodHound 
to map the attack surface and identify the vulnerable nodes during an engagement. However the analysis remains
a highly manual process, time consuming, heavely depandent on human expertise and none scalable 
in large enterprise environments.


\section*{Problem Statement}

Bloodhound maps the entirety of the Active Directory to a graph database, each node represents an object,
and the relationships between these objects is represented by edges.
When a user runs a path finding query, BloodHound simply runs Dijkstra over the attack graph,
it finds the shortest path, not necessarily the most realistic or discreet one.
\\The question this thesis asks : can a model learn a navigation policy and generalise
that policy to an unseen entreprise environment?

\section*{Objectives}

The objectif of this Thesis is to provide a proof of concept for learned attack path discovery 
over heterogeneous Active Directory graphs.
\\To that end this work aims to:

\begin{itemize}
  \item Build an end-to-end pipeline transforming raw BloodHound and Certipy JSON
  to navigable \gls{pyg} \texttt{HeteroData} tensors.
  \item Train a GNN-based navigation policy and demonstrate cross-environment generalisation 
  to unseen entreprise level environments.
  \item Identify and precisely describe the model's limitations and architectural boundaries 
  through systematic evaluation, establishing future work and improvements.
\end{itemize}


\section*{Main Contributions}
\begin{itemize}
  \item A complete, modular heterogeneous graph pipeline from raw BloodHound/Certipy
        JSON to navigable \gls{pyg} \texttt{HeteroData} tensors, covering
        \gls{adcs}, \gls{rbcd}, GPLink, and \gls{sid} history edges.
  \item A \gls{hgt}-based navigation policy trained on \gls{goad} traces and
        evaluated on the entirely unseen INLANEFREIGHT enterprise environment,
        demonstrating generalisation from a 351-node lab to a 4\,000-node network.
  \item A diagnostic evaluation of the learned policy's architectural 
        boundaries identifying how each design oversight \gls{dcsync} 
        goal-state blindness, computer-node edge directionality, and 
        training-distribution sparsity propagated into observable 
        failure modes, and estimating the performance ceiling imitation 
        learning could reach if these constraints were lifted.
  \item A forward-looking system hypothesis describing a second-generation 
        tool where identified oversights are addressed: a goal-conditioned 
        policy with \gls{dcsync}-aware terminal learning, bidirectional 
        computer-node traversal, and an expanded training corpus 
        outlining what the approach is capable of reaching beyond the 
        bounds of this proof of concept.
\end{itemize}

\section*{Report Outline}
The remainder of this document is structured as follows. 
Chapter 1 establishes the theoretical foundation, reviewing the architecture of Active Directory, 
common privilege escalation techniques, the machine learning logic behind GNNs and the current 
state of graph-based attack path analysis.
Chapter 2 details the methodology and system architecture, introducing the heterogeneous graph 
pipeline designed to transform raw BloodHound data into navigable tensors and present the core logic 
of the navigation policy the loss function and the beam search method. 
Chapter 3 presents the architecture and training paradigm of the Graph Neural Network (GNN) 
navigation policy. 
Chapter~4 provides an empirical evaluation of the learned policy across 
multiple environments, documents both successful generalisations and precise 
failure modes, and identifies the architectural oversights that bound the 
current approach.
Chapter~5 concludes the report by reviewing what this proof of concept 
achieved and laying out its limitations honestly. It then builds a hypothesis
where a second generation tool, one where the identified oversights are properly handled,
could theoretically reach as well as pinpointing where future research might target.


%% ============================================================
%%  CHAPTER 1 — BACKGROUND AND RELATED WORK
%% ============================================================
\chapter{Background and Related Work}

\section{Active Directory: Architecture and Security Model}

\subsection{Core Objects and Structure}
% Cover: users, computers, OUs, GPOs, domains, forests, trusts.
% One diagram of the hierarchical AD structure (forest → domain → OU → objects)
% is worth inserting here.

Active Directory (AD) a directory service for Windows network environments. It is a distributed, 
hierarchical structure that allows for centralized management of an organization's resources, 
including users, computers, groups, network devices, file shares, group policies, servers 
and workstations, and trusts. AD provides authentication and authorization functions within 
a Windows domain environment.\href{https://academy.hackthebox.com/module/74/section/1347}{1}
\\
\\
\begin{figure}[H]
  \centering
  \textbf{\color{navyblue} Active Directory Object Hierarchy} \\[0.5em]
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[node distance=0.5cm and 0.5cm]

  %\node[diagramtitle] (T) {Active Directory Object Hierarchy};

  %% Forest
  \node[forestbox, below=0.7cm] (forest)
    {Forest\quad\texttt{corp.local}};

  %% Domains
  \node[domainbox, below left=1.0cm and 2.2cm of forest] (d1)
    {Domain\\[1pt]\texttt{corp.local}};
  \node[domainbox, below right=1.0cm and 2.2cm of forest] (d2)
    {Child Domain\\[1pt]\texttt{eu.corp.local}};

  \draw[thesisarrow] (forest.south) -- ++(0,-0.28) -| (d1.north);
  \draw[thesisarrow] (forest.south) -- ++(0,-0.28) -| (d2.north);

  %% OUs under d1
  \node[oubox, below left=1.0cm and 0.8cm of d1] (ou1) {OU: IT Dept};
  \node[oubox, below right=1.0cm and 0.8cm of d1] (ou2) {OU: Finance};

  \draw[thesisarrow] (d1.south) -- ++(0,-0.22) -| (ou1.north);
  \draw[thesisarrow] (d1.south) -- ++(0,-0.22) -| (ou2.north);

  %% OU under d2
  \node[oubox, below=1.0cm of d2] (ou3) {OU: Servers};
  \draw[thesisarrow] (d2.south) -- (ou3.north);

  %% Objects under ou1
  \node[objbox, below left=0.9cm and 0.4cm of ou1] (u1)
    {\textbf{User}\\[1pt]john.doe};
  \node[objbox, below right=0.9cm and 0cm of ou1] (s1)
    {\textbf{Svc Account}\\[1pt]svc-mssql};

  \draw[thesisarrow] (ou1.south) -- ++(0,-0.2) -| (u1.north);
  \draw[thesisarrow] (ou1.south) -- ++(0,-0.2) -| (s1.north);

  %% Objects under ou2
  \node[objbox, below left=0.9cm and 0cm of ou2] (g1)
    {\textbf{Group}\\[1pt]Finance-Admins};
  \node[objbox, below right=0.9cm and 0.4cm of ou2] (gpo)
    {\textbf{GPO}\\[1pt]PW-Policy};

  \draw[thesisarrow] (ou2.south) -- ++(0,-0.2) -| (g1.north);
  \draw[thesisarrow] (ou2.south) -- ++(0,-0.2) -| (gpo.north);

  %% Objects under ou3
  \node[objbox, below=0.9cm of ou3] (c1)
    {\textbf{Computer}\\[1pt]SRVSQL01};
  \draw[thesisarrow] (ou3.south) -- (c1.north);

  %% GPLink dashed
  \draw[dasharrow, bend right=30] (gpo.west)
    to node[arrowlabel, left=3pt] {GPLink} (ou2.south);

  %% Legend
  \begin{scope}[shift={(7.5,-3.5)}]
    \node[font=\small\bfseries\color{labelgray}] (lt) {Legend};
    \node[forestbox, minimum width=2.0cm, minimum height=0.5cm,
          font=\scriptsize\bfseries\color{forestgreen},
          below=0.15cm of lt] (ll1) {Forest};
    \node[domainbox, minimum width=2.0cm, minimum height=0.5cm,
          font=\scriptsize\bfseries\color{navyblue},
          below=0.15cm of ll1] (ll2) {Domain};
    \node[oubox, minimum width=2.0cm, minimum height=0.5cm,
          font=\scriptsize\color{oucolor},
          below=0.15cm of ll2] (ll3) {OU};
    \node[objbox, minimum width=2.0cm, minimum height=0.5cm,
          below=0.15cm of ll3] (ll4) {Object};
  \end{scope}

\end{tikzpicture}
}
  \caption{Active Directory object hierarchy.}
  \label{fig:ad-hierarchy}
\end{figure}

A basic AD user account with no added privileges can be used to enumerate the majority of objects 
contained within AD, including but not limited to
% TODO: Finish literature review

\begin{itemize}
  \item Domain Computers
  \item Domain Group Information
  \item Default Domain Policy
  \item Password Policy
  \item Domain Trusts
  \item Domain Users
  \item Organizational Units (OUs)
  \item Functional Domain Levels
  \item Group Policy Objects (GPOs)
  \item Access Control Lists (ACLs)
\end{itemize}


\subsection{Authentication Protocols}
% Kerberos: TGT/TGS flow, delegation types (unconstrained, constrained, RBCD).
% NTLM: challenge-response, relevance to lateral movement.
% Keep it concise — this is background, not a Kerberos tutorial.
\todo{Write authentication section}

\subsection{Trust Relationships}
% Intra-domain, cross-domain (SameForestTrust), cross-forest (TrustedBy).
% Explain why trusts create transitive attack paths and why SharpHound version
% differences (TrustedBy vs. SameForestTrust naming) matter for the pipeline.
\todo{Write trust relationships section}

% ─────────────────────────────────────────────────────────────
\section{Privilege Escalation in AD Environments}

\subsection{Offensive ACL Abuse}
% WriteDACL, GenericAll, GenericWrite, ForceChangePassword, AddMember.
% Explain what each right enables in an attack chain.
\todo{Write ACL abuse section}

\subsection{DCSync Attack}
% Required rights: GetChanges AND GetChangesAll on the domain object.
% What DCSync enables (secret replication, credential extraction).
% Why it is used as the terminal goal state in this work.
\todo{Write DCSync section}

\subsection{ADCS Attack Paths}
% Certificate-based privilege escalation: ESC1, ESC8.
% Why Certipy data is injected into the graph in the stitcher step.
\todo{Write ADCS section}

\subsection{SID History Abuse and RBCD}
% HasSIDHistory: cross-domain privilege inheritance.
% AllowedToAct (RBCD): resource-based constrained delegation as an attack primitive.
\todo{Write SIDHistory and RBCD section}

% ─────────────────────────────────────────────────────────────
\section{Existing Tools and Their Limitations}

\subsection{BloodHound and SharpHound}
% BloodHound: Neo4j-backed, Cypher query interface, built-in pathfinding (Dijkstra/BFS).
% SharpHound C#: comprehensive collection, cross-forest MemberOf support.
% Why Dijkstra is insufficient: no stealth scoring, no sequential decision model,
% no ability to weight discreetness of individual edge types.
\todo{Write BloodHound/SharpHound section}

\subsection{bloodhound-python}
% Python collector: easier deployment, no Windows dependency.
% Key limitations: does not capture foreign user-level MemberOf across forests,
% Trusts array requires explicit parsing to recover trust edges.
% These limitations directly bound what the model can discover — documented
% honestly as a data collection constraint, not a model flaw.
\todo{Write bloodhound-python limitations}

\subsection{Other Automated Attack Path Tools}
% Brief survey: Planner-based tools (PRISM, MulVAL), RL-based pen-testing agents
% (if any relevant prior work). Keep short — show the gap this work fills.
\todo{Write related tools survey}

% ─────────────────────────────────────────────────────────────
\section{Graph Neural Networks}

\subsection{Message Passing and Neighbourhood Aggregation}
% Core GNN formulation: aggregate → combine → update.
% Why the graph structure of AD (heterogeneous node/edge types) requires
% more than a vanilla homogeneous GNN.
\todo{Write GNN fundamentals}

\subsection{Graph Convolutional Network (GCN)}
% Kipf \& Welling formulation.
% Role in this work: homogeneous baseline.
% Why expressiveness on heterogeneous graphs is limited.
\todo{Write GCN section}

\subsection{Heterogeneous Graph Transformer (HGT)}
% Hu et al. (2020): per-meta-relation attention, type-level linear projections.
% Why this maps naturally to AD graphs: different relation types
% (MemberOf, WriteDACL, AllowedToDelegate...) carry fundamentally different semantics.
% Architecture choices made: 64D, 4 heads, 2 layers.
\todo{Write HGT section}

\subsection{PyTorch Geometric and HeteroData}
% Brief: HeteroData format, edge\_index per (src\_type, edge\_type, dst\_type),
% why this is the natural container for the AD graph.
\todo{Write PyG section}

% ─────────────────────────────────────────────────────────────
\section{Summary and Positioning}
% Synthesise the gap: BloodHound finds paths, but cannot learn which paths are
% preferable under a cost model. GNN navigation over heterogeneous AD graphs
% has not been addressed in prior work. This thesis is a PoC establishing
% feasibility and demonstrating cross-environment generalisation.
While recent work has explored GNN-based modelling of attacker 
behaviour in AD graphs from a \textit{defensive} perspective --- 
approximating attacker decision-making to guide edge-blocking 
strategies~\cite{goel2025coevolutionary} --- the complementary 
offensive direction remains largely unexplored. This thesis 
addresses that gap by learning an offensive navigation policy 
directly from labelled attack traces, evaluated on real enterprise 
environments rather than synthetic graphs.
\todo{Write summary and positioning}


%% ============================================================
%%  CHAPTER 2 — SYSTEM ARCHITECTURE AND DESIGN
%% ============================================================
\chapter{System Architecture and Design}

\section{System Overview}
% Present the full pipeline as a diagram (replicate or adapt the Mermaid diagram
% from the README as a TikZ figure or imported PNG).
% Describe the three operational modes:
%   1. Data preparation only:   ./launch.sh ./input ./output
%   2. Preparation + query:     ./launch.sh ./input ./output --start X --target Y
%   3. Inference only:          ./launch.sh ... --skip-prep
% State the principal design decision: BloodHound-standard principal→target
% edge direction throughout.
\todo{Write system overview and include pipeline diagram}

% ─────────────────────────────────────────────────────────────
\section{Data Collection and Preprocessing}

\subsection{Input Sources}
% BloodHound JSON (domains, users, computers, GPOs, ACLs, trusts).
% Certipy JSON (ADCS templates and CA objects).
\todo{Write input sources section}

\subsection{\tool{clean\_bloodhound.py}: Filtering and Multi-Domain Accumulation}
% Purpose: filter raw BloodHound exports, accumulate data across multiple files
% and domains without overwriting.
% Critical bug fixed: original code used assignment (\texttt{all\_data[key] = data["data"]})
% which silently collapsed 3 domains into 1. Fixed to use \texttt{.extend()}.
% Added recursive directory walk for nested scan directories.
\todo{Write clean\_bloodhound.py section}

\subsection{\tool{merger.py}: Normalisation and Merging}
% Lowercases all edge type names to resolve casing inconsistencies
% (e.g. WriteDACL vs. WriteDacl across SharpHound versions).
% Merges multi-domain data into a unified structure.
% Parses the Trusts array on domain objects — was previously ignored.
\todo{Write merger.py section}

\subsection{\tool{stitcher.py}: ADCS Injection and Domain Detection}
% Injects Certipy-derived ADCS nodes and edges into the merged graph.
% Auto-detects the target domain from the data rather than requiring manual input.
\todo{Write stitcher.py section}

% ─────────────────────────────────────────────────────────────
\section{Heterogeneous Graph Construction}

\subsection{Node and Edge Type Schema}
% List all node types (User, Computer, Group, Domain, GPO, OU, CertTemplate...).
% List all edge types with directions:
%   MemberOf, HasSession, GenericAll, WriteDACL, ForceChangePassword,
%   AllowedToDelegate, AllowedToAct, HasSIDHistory,
%   GPLink (GPO→container), TrustedBy/SameForestTrust, ADCS edges.
% A table here is effective.
\todo{Write node/edge schema — include a table}

\subsection{Direction Convention}
% Principal → Target (BloodHound standard).
% Why this matters: navigation traverses outgoing edges from the current node.
% Consequence: outgoing-only beam search; no "(reverse)" edge tags needed.
\todo{Write direction convention section}

\subsection{Edge Extraction Details}
% GPLink bug fix: BloodHound stores the GUID key, not ObjectIdentifier.
%   Direction was also reversed (was container→GPO, fixed to GPO→container).
% AllowedToDelegate: extracted from the allowedtodelegate attribute array.
% AllowedToAct: from the allowedtoact attribute (RBCD).
% HasSIDHistory: from sidhistory array on user/computer objects.
\todo{Write edge extraction details}

\subsection{PyG HeteroData Encoding}
% How node features are constructed (one-hot type encoding, or feature-less
% with learned embeddings).
% How edge\_index tensors are built per meta-relation tuple
% (src\_type, edge\_type, dst\_type).
\todo{Write PyG encoding section}

% ─────────────────────────────────────────────────────────────
\section{Navigation Model}

\subsection{HGT Encoder}
% Architecture: 64-dimensional hidden size, 4 attention heads, 2 layers.
% Role: produce node embeddings that encode heterogeneous neighbourhood context.
% Why HGT over GCN here: per-meta-relation attention captures the semantic
% difference between, e.g., a MemberOf edge and a WriteDACL edge.
\todo{Write HGT encoder section}

\subsection{Policy Head}
% MLP on top of the HGT embeddings.
% Input: current node embedding.
% Output: softmax over candidate next-hop neighbours.
\todo{Write policy head section}

\subsection{Loss Function and Step Cost}
% Weighted cross-entropy:
%   eff\_weight = path\_weight × (1 − step\_cost)
% path\_weight: importance of the training trace.
% step\_cost: encodes the noisiness / detectability of an edge type.
% Why this produces paths with lower cumulative cost than Dijkstra on the
% same graph — the key claim supporting the stealth argument.
\todo{Write loss function section}

% ─────────────────────────────────────────────────────────────
\section{Beam Search Navigation}

\subsection{Algorithm}
% Describe the beam search loop: expand top-k candidates, score with policy head,
% prune to beam width, repeat.
\todo{Write beam search algorithm description — consider an Algorithm2e pseudocode block}

\subsection{Terminal State: DCSync}
% A node satisfies the terminal condition when it holds GetChanges AND
% GetChangesAll on the target domain object.
% Beam search freezes that hypothesis and stops expanding it.
% Design rationale: DCSync as an absorbing goal state, not a post-hoc trim.
\todo{Write terminal state section}

\subsection{Cross-Environment Inference}
% The HGT checkpoint is patched with training\_metadata
% (list of node types, list of edge type tuples seen during training).
% At inference time, the HGT is rebuilt from this metadata.
% Edge types present in the new environment but absent from training receive
% empty (zero) tensors — the model still traverses them via the beam search
% topology without dedicated learned parameters.
\todo{Write cross-environment inference section}

% ─────────────────────────────────────────────────────────────
\section{Audit Module}
% Three-level output: \textbf{ok} / \textbf{warn} / \textbf{fail}.
% Checks performed:
%   (1) Path reaches declared target OR a mid-path DCSync node is found.
%   (2) Final node domain matches target domain.
%   (3) No trailing structural noise edges (MemberOf/Contains at path end).
%   (4) Single-step degenerate path detection.
% Honest scope: static heuristic flagging, not a substitute for operator judgement.
\todo{Write audit module section}


%% ============================================================
%%  CHAPTER 3 — IMPLEMENTATION
%% ============================================================
\chapter{Implementation}

\section{Technical Stack}
% Python 3.x, PyTorch Geometric, Streamlit, pyvis, networkx.
% Training: Kaggle notebooks, GPU T4 ×2 (note P100 sm\_60 incompatibility
% with newer PyTorch — reason for T4).
% Version-pin key dependencies in a small table (torch, torch-geometric,
% streamlit, pyvis).
\todo{Write technical stack section — include a versions table}

\section{Repository Structure}
% Reproduce the directory tree from the README.
% Brief one-line description per top-level file/folder.
% Note .gitignore paranoia: no real AD scans ever committed.
\todo{Write repository structure section — include the directory tree as a listing}

% ─────────────────────────────────────────────────────────────
\section{Data Pipeline}

\subsection{Script Responsibilities and Data Flow}
% A table mapping each script → input → output → key logic is effective here.
\todo{Write pipeline data flow section — include input/output table}

\subsection{SharpHound Version Normalisation}
% TrustedBy vs. SameForestTrust: aliased in build\_dataset.py.
% WriteDACL vs. WriteDacl: resolved by lowercase normalisation in merger.py.
% These are documented as known fragilities, not bugs.
\todo{Write SharpHound normalisation section}

% ─────────────────────────────────────────────────────────────
\section{Training}

\subsection{Training Data: GOAD}
% GOAD (Game of Active Directory): multi-domain lab environment.
% 33 annotated attack traces (zoom.csv).
% What the traces teach: ACL chains, group inheritance paths, DCSync terminal.
% What they do NOT teach: GPLink chains, RBCD, Kerberoasting, shadow credentials.
% This bound is explicitly acknowledged and becomes the coverage ceiling.
\todo{Write GOAD training data section}

\subsection{Kaggle Training Notebook}
% Structure of the training notebook.
% Cell 5.5: checkpoint patch — saves training\_metadata alongside model weights
%   so the HGT can be reconstructed faithfully in a different environment.
\todo{Write Kaggle notebook section}

\subsection{Model Checkpoints}
% GCN.pt, HGT.pt (with metadata), HGT-no-metadata.pt (backup).
% Why the metadata patch was necessary: without it, inference in a new environment
% with different node/edge type sets caused silent architectural mismatch.
\todo{Write model checkpoints section}

% ─────────────────────────────────────────────────────────────
\section{Inference Pipeline}
% inference.py flow: load checkpoint → rebuild HGT from metadata →
%   load HeteroData → run beam search → run audit → write log.
% launch.sh orchestration: how the three modes are dispatched.
\todo{Write inference pipeline section}

% ─────────────────────────────────────────────────────────────
\section{Graphical Interface}

\subsection{Design and Theme}
% Streamlit application with professional office-app theme.
% Colour palette: navy \#1f4e79, slate and maroon accents — no neon.
% Rationale: tool intended for professional red-team context.
\todo{Write GUI design section}

\subsection{Workflow Integration}
% Folder picker (tkinter native popup, text fallback).
% launch.sh wrapped as subprocess; stdout streamed live to the UI.
% Auto-skip prep if forest\_graph.json + heterodata.pt already exist.
% @st.cache\_resource for model and graph loading.
\todo{Write GUI workflow section}

\subsection{Visualisation}
% pyvis graph: hierarchical left-to-right layout, height 520 px.
% Muted colour palette (no neon) — each node type distinct colour.
% Step-by-step table in collapsible expander below the graph.
% Inference logs persisted to output/inference\_<timestamp>.log.
\todo{Write GUI visualisation section}


%% ============================================================
%%  CHAPTER 4 — EXPERIMENTS AND RESULTS
%% ============================================================
\chapter{Experiments and Results}

\section{Experimental Setup}

\subsection{Training Environment: GOAD}
% Game of Active Directory: ~3 domains, deliberately vulnerable AD lab.
% Used exclusively for training.
% 33 traces define the coverage ceiling of the model.
Game of Active Directory, a deliberately vulnerable Active Directory lab, created for penetration 
testers to practice common infrastructure attacks and privilege escalation techniques.
GOAD consists of three domains --- 
\texttt{sevenkingdoms.local}, \texttt{north.sevenkingdoms.local}, and 
\texttt{essos.local} --- interconnected through domain trust relationships, 
mimicking a realistic multi-domain enterprise architecture.
The lab exposes various sorts of misconfigurations commonly found in enterprise environments,
from unpatched system vulnerabilies to unprotected services and network misconfigurations.
The attack paths goad provides are to be presented in a learnable and readable training format for the model.
Each attack path is deconstructed into transitions, current\_node, next\_hope, credentials\_used, total 
path\_weight, train\_on\_step, notes and the step\_cost.

\subsection{Feature engineering}

To a GNN model learning from a bloodhound scan, attack paths are of different importance, 
some are totaly invisible while others are a gold mine for what the model needs to replicate.
Paths that rely on unpatched vulnerabilities or outdated systems, for instance, leave no trace in the 
BloodHound graph and are marked with a \texttt{path\_weight} of 0.
Paths that are mostely visible with only two or less transitions that do not qualify have the latter marked
through train\_on\_step set to zero.
And at last, for an imitation learning model, given the choice between two valide transitions, the model
choses based on training data, prefering the edge type it has seen most during training. To address this
We are to more or less guide  the model to gravitate towards the edge attack with less impact on 
the environment, the one with less noise and impact. To reach the desired effect each valid transition
is scored. This score is reflected directly in the loss function through the \texttt{step\_cost} feature.

In summary, the feature engineering phase introduced three mechanisms: 
filters to eleminate CVE-dependent transitions. Weighted paths by a composite visibility score
giving priority to transitions where the graph structure is the primairy escalation vector.
Finally a step\_cost feature helping guide the model towards the stealthier less impactful 
edge in case of a choice presenting itself.
This constitutes a simplified reward shaping approach consistent with the imitation learning literature.


\subsection{Test Environment: INLANEFREIGHT}
% HTB Academy enterprise AD lab: ~4\,000 nodes, multiple domains, cross-domain trusts.
% Never seen during training — all results on this environment are
% genuine zero-shot generalisation.
% Data acquired via bloodhound-python; ADCS via Certipy.
\todo{Write INLANEFREIGHT setup section}

\subsection{Test Environment: GOAD castelblack / kingslanding}
% Used specifically to test constrained delegation edge (AllowedToDelegate).
% Edge type extracted after pipeline patch — model not retrained.
\todo{Write castelblack/kingslanding setup}

\subsection{Coverage Metric}
% To avoid the cherry-picking reading: report N queries attempted,
% M valid paths recovered, audit pass rate.
% Honest framing: failures on jon.snow, samwell.tarly etc. are operating
% correctly at the training-set coverage edge — not regressions.
\todo{Write coverage metric section — include a summary table of all queries}

% ─────────────────────────────────────────────────────────────
\section{Result 1: wley $\rightarrow$ Domain Admin on INLANEFREIGHT}
\label{sec:result1}

% This is the headline result. Give it space.
%
% Subsections:
%   - Query and context
%   - Recovered path (list the nodes and edge types step by step)
%   - Score: HGT 0.5620
%   - Comparison with BloodHound shortest path on the same graph
%   - KEY FIGURE: side-by-side path comparison with step-cost gap
%   - Claim: intra-domain generalisation, 351-node lab → 4000-node enterprise
\todo{Write Result 1 — include the key comparison figure}

% Suggested figure structure:
% \begin{figure}[H]
%   \centering
%   \includegraphics[width=\textwidth]{result1_path_comparison}
%   \caption{Learned path vs.\ BloodHound shortest path for wley→Domain Admin
%            on INLANEFREIGHT. The learned policy selects a longer but lower
%            step-cost route, avoiding high-noise edges.}
%   \label{fig:result1}
% \end{figure}

% ─────────────────────────────────────────────────────────────
\section{Result 2: Cross-Domain Trust Traversal}
\label{sec:result2}

% Query: htb-student\_adm → INLANEFREIGHT domain
% Score: 0.2349
% Path: show the trust traversal steps
% Analysis: structural demonstration that the model navigates trust edges.
% Honest note: lower score reflects data collection limitations on trust edges
%   (bloodhound-python Trusts array partially captured) — not model failure.
\todo{Write Result 2}

% ─────────────────────────────────────────────────────────────
\section{Result 3: Constrained Delegation — castelblack $\rightarrow$ kingslanding}
\label{sec:result3}

% Query: castelblack → kingslanding
% Path: castelblack → winterfell (AllowedToDelegate) →
%        Enterprise DCs (MemberOf) → sevenkingdoms.local (DCSync)
% Score: 0.1685
%
% Key insight to foreground:
%   The AllowedToDelegate edge type was NOT present in training data.
%   It was added to the graph by the build\_dataset.py pipeline patch.
%   The model discovers the path WITHOUT retraining.
%   Contribution: pipeline completeness drives discoverability — the model
%   traverses the new edge via beam search topology even without dedicated
%   learned parameters for that meta-relation.
%
% Known limitation: beam search continues past the DCSync hit at step 3
%   for 3 additional noise steps. Identified as future reconception work
%   (absorbing terminal state inside the beam loop).
\todo{Write Result 3}

% ─────────────────────────────────────────────────────────────
\section{Result 4: Documented Limitation — Cross-Forest Foreign MemberOf}
\label{sec:result4}

% Query: administrator@inlanefreight → admins@freightlogistics
% Outcome: fails — model produces a degenerate 1--2 step path.
% Root cause: bloodhound-python does not capture foreign user-level MemberOf
%   across forest boundaries. The edge simply does not exist in the graph,
%   so the model cannot traverse it regardless of capability.
% Framing: this is a DATA COLLECTION limitation, not a model regression.
%   Requires SharpHound C# for cross-forest foreign group collection.
\todo{Write Result 4}

% ─────────────────────────────────────────────────────────────
\section{Discussion}

\subsection{Model Coverage and Training-Set Ceiling}
% 33 traces teach ACL chains + group inheritance + DCSync.
% The model never sees GPLink chains, RBCD, Kerberoasting in training.
% Failure on out-of-distribution queries is expected and correct behaviour.
% The model is NOT broken — it operates correctly at its coverage edge.
\todo{Write coverage discussion}

\subsection{Expressiveness vs.\ Transferability: HGT vs.\ GCN}
% HGT > GCN on training distribution (GOAD).
% GCN $\geq$ HGT on cross-environment transfer (INLANEFREIGHT).
% Interpretation: HGT's per-relation expressiveness overfits slightly to
%   training meta-relations; GCN's simplicity transfers more robustly.
%   Both recover the correct path — the gap is in raw scores, not correctness.
\todo{Write HGT vs. GCN discussion}

\subsection{Pipeline Completeness as a First-Class Contribution}
% Result 3 demonstrates that expanding what the pipeline extracts
%   directly expands what the model can discover — without retraining.
%   This decoupling of graph coverage from model capability is a design
%   insight worth foregrounding in the thesis narrative.
\todo{Write pipeline completeness discussion}


%% ============================================================
%%  CHAPTER 5 — LIMITATIONS AND FUTURE WORK
%% ============================================================
\chapter{Limitations and Future Work}

\section{Data Collection Incompleteness}
\label{sec:limit-data}

% Open with the concrete case: the administrator@inlanefreight →
% admins@freightlogistics query fails not because the model lacks capability
% but because the edge is absent from the collected graph.
%
% bloodhound-python limitations to discuss:
%   - Foreign user-level MemberOf across forest boundaries not captured
%   - Trusts array requires explicit parsing (was initially ignored by merger)
%   - No CoerceToTGT-style derived edges
%
% What SharpHound C# provides:
%   - Full cross-forest object collection including foreign group membership
%   - Richer ACL enumeration
%   - More complete GPLink and delegation data
%
% Implication for future iterations: C# collection is not optional for
% cross-forest attack path completeness.
\todo{Write data collection limitations section}

% ─────────────────────────────────────────────────────────────
\section{Expanding the Training Knowledge Base}
\label{sec:limit-training}

% Current training set: 33 traces from GOAD.
% What those traces teach: ACL chains, group inheritance, DCSync terminal.
% What they do NOT teach: RBCD (AllowedToAct), shadow credentials,
%   Kerberoasting, GPLink-based chains, certificate abuse chains (ESC1/ESC8).
%
% Two expansion strategies:
%
% 1. Synthetic trace injection:
%    Generate labelled attack traces for new primitives (RBCD, shadow credentials)
%    in a controlled lab (GOAD extension or custom Vagrant environment).
%    Even 5--10 new traces per primitive would give the model a training signal.
%
% 2. Corpus augmentation from public sources:
%    HTB writeups, CRTE/CRTO course materials (if licensable), RedTeam notes.
%    Requires careful anonymisation and edge-type normalisation.
%
% Note: retraining on current data with the new edge types (gplink=8,
% allowedtodelegate=4) was evaluated and skipped — volume too low for a
% meaningful training signal. This decision is documented here as a
% deliberate choice, not an oversight.
\todo{Write training knowledge base expansion section}

% ─────────────────────────────────────────────────────────────
\section{Terminal State Learning via Offline Reinforcement Learning}
\label{sec:limit-rl}

% Current design: DCSync terminal is a hardcoded symbolic predicate
%   (GetChanges + GetChangesAll on the target domain).
% Problem: the beam search does not internalise the goal — it navigates
%   greedily then trims at the first terminal hit. This produces the
%   noise-tail issue observed in Result 3.
%
% Proposed future direction: offline RL over labelled rollouts.
%   - Label DCSync nodes as terminal states in the training traces.
%   - Train a value head (critic) that assigns high value to nodes with
%     DCSync rights on the target domain.
%   - The policy head learns to steer toward high-value nodes intrinsically,
%     without a hardcoded predicate.
%   - Algorithms: conservative Q-learning (CQL) or implicit Q-learning (IQL)
%     are well-suited to offline (batch) settings where online interaction
%     with a live AD environment is not feasible.
%
% Short-term interim: absorbing terminal state inside the beam loop
%   (freeze hypothesis at first DCSync hit, stop expanding) —
%   implementable without retraining, addresses the noise-tail immediately.
\todo{Write offline RL future direction section}

% ─────────────────────────────────────────────────────────────
\section{Additional Future Directions}

\subsection{Calibrated Confidence Scoring}
% Current audit is a three-level heuristic (ok/warn/fail).
% Future: calibrate the softmax output scores against empirical path success
%   rates so that a score of 0.X corresponds to a meaningful probability
%   of reaching the target in a real environment.
\todo{Write calibrated confidence section}

\subsection{SharpHound Edge Vocabulary Versioning}
% SharpHound edge type names evolve across versions (TrustedBy vs.
% SameForestTrust, WriteDACL vs. WriteDacl).
% A versioned alias map in the pipeline would make normalisation explicit
% and maintainable as BloodHound/SharpHound evolve.
\todo{Write edge vocabulary versioning section}

\subsection{Derived Attack Primitives}
% CoerceToTGT-style edges (PetitPotam, PrinterBug) are not captured by
%   any collector — they require protocol-level interaction.
% Future pipeline could infer coercibility from machine exposure
%   (e.g., spooler service running + no SMB signing) as a synthetic edge.
\todo{Write derived primitives section}


%% ============================================================
%%  GENERAL CONCLUSION
%% ============================================================
\chapter*{General Conclusion}
\addcontentsline{toc}{chapter}{General Conclusion}
\markboth{General Conclusion}{}

% Structure (roughly one paragraph each):
%
% 1. Restate what was built: an end-to-end pipeline + HGT-based navigation
%    policy for AD privilege escalation, from raw BloodHound JSON to
%    beam-search inference with audit output and a Streamlit GUI.
%
% 2. Three defensible contributions:
%    (a) Cross-environment generalisation: wley→DA on a 4000-node enterprise
%        never seen during training.
%    (b) Trust-aware navigation: structural demonstration of cross-domain
%        path discovery.
%    (c) Pipeline completeness as a research lever: the AllowedToDelegate
%        result was unlocked by a pipeline patch, not model retraining.
%
% 3. Honest scope: this is a PoC. 33 training traces define the coverage
%    ceiling. The model operates correctly within those bounds and fails
%    gracefully outside them.
%
% 4. Forward look: the identified limitations (data collection, training
%    corpus size, hardcoded terminal heuristic) each have a clear
%    research path forward, making this work a foundation rather than
%    a dead end.
\todo{Write general conclusion (~1 page)}


%% ============================================================
%%  BIBLIOGRAPHY
%% ============================================================
\printbibliography[heading=bibintoc, title={Bibliography}]


%% ============================================================
%%  APPENDICES
%% ============================================================
\appendix
\chapter{Sanitised BloodHound JSON Example}
\label{app:json}
% Show a small anonymised excerpt of a BloodHound domain JSON
% (users array, ACEs array) to give the reader concrete intuition
% of the raw input format.
\todo{Insert sanitised JSON excerpt as a lstlisting}

\chapter{Heterogeneous Graph Schema}
\label{app:schema}
% Full table of node types and edge types with directions and
% source scripts. Reference this from Chapter 2.
\todo{Insert full graph schema table}

\chapter{Key Code Excerpts}
\label{app:code}

\section*{Effective Weight Loss Function}
\begin{lstlisting}[language=Python, caption={Effective weight computation in \tool{prepare\_training\_examples.py}}]
# eff_weight combines path importance with step discretion cost
eff_weight = path_weight * (1 - step_cost)
\end{lstlisting}
\todo{Add beam search loop and has\_dcsync\_on excerpts here}

\chapter{GUI Screenshots}
\label{app:gui}
% Four screenshots:
%   1. Main interface / folder picker
%   2. wley→DA path visualisation (pyvis graph)
%   3. Step-by-step table expander
%   4. Audit output panel
\todo{Insert GUI screenshots}

\end{document}